# Write a Python program which accepts the radius of a circle from the user
# and compute the area

pi = 3.14
radius = float(input("Enter the radius of circle: "))

print("Area of circle = {}".format(pi * radius * radius))
